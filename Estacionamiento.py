

class Vehiculo():
    
    def __init__(self):
        """ Iniciando Una Lista """
        self.vehiculos = list()

    def nuevo_vehiculo():
        """ Manda un redistro A La Lista """
        self.vehiculos.append(Note(memo, tags))

    def _search_note(self, note_id):
        for note in self.notes:
            if str(note.id) == str(note_id):
                return note

        return None
